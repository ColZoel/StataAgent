pydantic-ai>=1.87.0
pystata~=0.0.1
pydantic-ai-litellm  # unified LLM provider backend; replaces direct openai SDK dependency
pyyaml>=6.0     # config.yaml parsing
python-dotenv>=1.0  # load API keys from .env files
pyfiglet>=1.0   # ASCII art banner
rich>=13.0      # styled terminal output
pydantic~=2.13.3
charset_normalizer>=3.0  # required by requests for character detection
fastapi>=0.110   # web UI backend (used when ui: true in config.yaml)
uvicorn>=0.29    # ASGI server for the web UI
